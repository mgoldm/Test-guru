<tr>
  <td><%= badge.title %></td>
  <td><%= octicon "#{badge.file_name}", class: "first test success" %></td>
  <td><%= link_to 'Delete', admin_badge_path(badge),
                  method: :delete,
                  data: { confirm: 'Do you want to delete this badge' } %></td>
</tr>
