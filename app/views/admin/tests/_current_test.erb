<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td>
    <% test.questions.each_with_index do |question, index| %>
      <ul><%= index + 1 %> <%= link_to "#{question.title}", admin_question_path(question) %></ul>
    <% end %></td>
  <td><%=link_to 'Edit', edit_admin_test_path(@test) %></td>
  <td><%= link_to 'Back', admin_tests_path %></td><%= %>
</tr>
