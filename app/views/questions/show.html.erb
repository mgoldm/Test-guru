<h1><%= render plain: @question.inspect %></h1>

<ul>
  <li><%= link_to "Destroy", question_path(@question),
                  method: :delete,
                  data: { confirm: "Do you want to delete this question?" } %>

</ul>
